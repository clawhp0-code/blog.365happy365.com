name: Daily Product Post PM

on:
  schedule:
    - cron: "0 11 * * *"  # 매일 20:00 KST (UTC 11:00)
  workflow_dispatch:

jobs:
  generate-post:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Generate product PM post
        run: node scripts/product-daily.mjs pm
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}

      - name: Commit and push
        run: |
          git config user.name "product-bot"
          git config user.email "product-bot@blog.365happy365.com"
          git add content/posts/product-*.mdx
          git diff --staged --quiet || git commit -m "content: add daily product post (pm)"
          git push origin main
